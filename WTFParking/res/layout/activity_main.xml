<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/pkMapButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/pkListButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:text="@string/parking_map_button" />

    <Button
        android:id="@+id/favButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/pkMapButton"
        android:layout_alignRight="@+id/pkMapButton"
        android:layout_below="@+id/pkMapButton"
        android:layout_marginTop="27dp"
        android:text="@string/favorite_button" />

    <Button
        android:id="@+id/filterButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/favButton"
        android:layout_alignRight="@+id/favButton"
        android:layout_below="@+id/favButton"
        android:layout_marginTop="28dp"
        android:text="@string/filter_button" />

    <Button
        android:id="@+id/todayPkButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/filterButton"
        android:layout_alignRight="@+id/filterButton"
        android:layout_below="@+id/filterButton"
        android:layout_marginTop="33dp"
        android:text="@string/today_parking_button" />

    <Button
        android:id="@+id/pkListButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/pkMapButton"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/pkMapButton"
        android:layout_marginTop="32dp"
        android:text="@string/parking_list_button" />

</RelativeLayout>